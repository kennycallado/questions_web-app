import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.kenycallado.questions-app',
  appName: 'questions-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
